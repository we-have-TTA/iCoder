<!DOCTYPE html>
<html>
  <head>
    <title><%= @title || "iCoder" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://kit.fontawesome.com/e4b5a19f36.js" crossorigin="anonymous"></script>
  </head>
  <body>
    <%= render "shared/header_dashboard" %>
    <% if notice %>
      <p class="notice"><%= render "shared/notice" %></p>
    <% else %>
      <p class="alert"><%= alert %></p>
    <% end %>
    <div class="flex h-screen">
      <div class="w-72 h-full bg-gray-200">
        <%= render "shared/sidebar" %>
      </div>
      <div class="w-full bg-gray-100 px-5">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
